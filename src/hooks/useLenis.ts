"use client";

/**
 * Native Smooth Scroll — Native browser wheel mechanics without artificial lag.
 */
export function useLenis() {
  // Native scrolling enabled for instant, fast, jitter-free performance on PC.
}
